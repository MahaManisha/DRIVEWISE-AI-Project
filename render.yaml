services:
  - type: web
    name: drivewise-dashboard
    runtime: static
    rootDir: dashboard
    buildCommand: npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_BASE_URL
        value: https://drivewise-monitor.loca.lt/api
